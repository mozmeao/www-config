apiVersion: v1
kind: Service
metadata:
    name: bedrock-demo-l10n-db
    namespace: bedrock-demo
spec:
    ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8000
    selector:
      app: bedrock-demo-l10n-db
      type: web
    type: ClusterIP
