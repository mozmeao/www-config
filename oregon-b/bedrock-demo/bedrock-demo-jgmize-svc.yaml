apiVersion: v1
kind: Service
metadata:
    name: bedrock-demo-jgmize
    namespace: bedrock-demo
spec:
    ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8000
    selector:
      app: bedrock-demo-jgmize
      type: web
    type: ClusterIP
