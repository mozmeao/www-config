apiVersion: v1
kind: Service
metadata:
    name: bedrock-demo-craigcook
    namespace: bedrock-demo
spec:
    ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8000
    selector:
      app: bedrock-demo-craigcook
      type: web
    type: ClusterIP
