apiVersion: v1
kind: Service
metadata:
    name: bedrock-demo-agibson
    namespace: bedrock-demo
spec:
    ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8000
    selector:
      app: bedrock-demo-agibson
      type: web
    type: ClusterIP
